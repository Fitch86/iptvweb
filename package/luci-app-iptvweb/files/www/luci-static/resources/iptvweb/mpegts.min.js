/*
 * mpegts.js 1.8.0 is installed locally by install.sh.
 * This file is intentionally only a bootstrap/fallback marker; install.sh
 * replaces it with the full distribution before enabling the player.
 */
window.__iptvwebMpegtsLocalRequired = true;