/*
 * mpegts.js 1.8.2 is installed locally by install.sh.
 * This source archive keeps a bootstrap marker; install.sh replaces it
 * with the full local distribution before enabling the player.
 */
window.__iptvwebMpegtsLocalRequired = true;
